enum LocalDataKeys {
  loggedIn,
  accessToken,
  userType,
  isDarkTheme,
  languageKey,
  unreadMessages,
  unreadNotifications,
  selleId,
  privacyPolicy,
  profile,
  package,
  name,
  offer,
  ads,
  activity,
  infoamationPackage,
  role
}

enum UserType {
  authenticated,
  onboarding,
  completedProfile,
  completedCommercial
}

enum LanguageKey { en, ar }
